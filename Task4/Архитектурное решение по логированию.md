# Какие логи нужно собирать
Frontend: Internet shop, MES, CRM  
API: Shop API, MES API, CRM API

nginx frontend и API  - access логи и логи ошибок

INFO
- Время логгируем всегда
- Все API. Лог изменения статуса заказа. *Заказ #%s. Изменен статус заказа %s->%s*
- Все API. Если есть уведомления пользователя о статусе заказа, логгировать транспорт, тип сообщения, маскируемый адрес назначения. *Заказ #%s. Отправлено уведомление типа %s типа %s на адрес назначения %s*
- Результат сохранения файла в S3. *Заказ #%s. Файл сохранен в %s.*
- MES API. *Заказ #%s рассчитан за %d минут*

ERROR
- Неуспех в случае сохранения в S3
- Неуспех отправки сообщения в очередь, коннекта с БД, отправки сообщения почтой и другими транспортами (слак, телеграм)


# Мотивация
Мотивация схожа с той которую я описывал для трейсинга.

- Для ряда случаев вместо локального воспроизведения ошибок на машине разработчика, достаточно будет посмотреть логи.
- Грамотно и полно реализованное логирование позволяет искать странные и редкие ошибки в приложении и решать их в корне.
- Внедрение логирования позволит без потери контроля добавлять новые элементы инфраструктуры и интеграции

Метрики на которые повлияет логирование
- Время исправления ошибки при взаимодействии с инфраструктурой и внешними интеграциями
- Среднее время на устранение ошибок
- Возможно: сокращение времени релизов за счет ускорения диагностики ошибок-блокеров

Пункт про невозможность покрытия логами и трейсингом мне непонятна. Все API нужно покрыть логированием и трейсингом. nginx сервисов можно покрыть логированием во вторую очередь.


# Предлагаемое решение
Использовать буду стэк Grafana, Loki, Promtail.

Promtail - ставиться на каждый сервис откуда нужно собирать логи
Loki – БД для логов
Grafana – UI поиска по логам

Это стек предпочитаю ELK т.к. Grafana уже присутствует в технологическом ланшафте. Плюс Loki экономичнее в плане ресурсов чем ElasticSearch.

Поиск по логам в Loki это диапазон времени + метки.

Для логов nginx настройка формата логов не требуется. Метки Loki: приложение=nginx, сервер, уровень ошибки.

Формат логов приложения, произвольная строка в зависимости от приложения. Для java это будет. Метки Loki: приложение, сервер, уровень ошибки.

## Политика хранения логов
- Хранение логов по приложению 2 месяца
- Специальная настройка индекса для Loki не требуется

## Безопасность
- Логирование должно быть изолировано от внешнего контура. Доступ к UI должен быть только из частной сети компании. Нужно учесть необходимость конфигурации сети т.к. система находится в облаке.
- Доступ для сотрудников отдела разработки: бэкенд-программисты, тестировщики, девопс. Роли: developer, qa, devops.
- Доступ для сотрудников отдела поддержки. Роль: support.


# Автоматический анализ логов
Пока предлагаю ограничиться следующим.

## Алертинг
- Для каждого API: Слишком много ошибок к внешнему ресурсу или сервису
## Поиск аномалий
- Слишком много 403 с IP
- Слишком много 403 на пользователя


# Критерии для выбора технологии
Фактически я пошел по пути не плоди лишних сущностей. Т.к. Grafana была уже в стеке я ориентировался на это.

Дополнил таблицу используя ИИ

|Критерий|ELK (Elastic Stack)|OpenSearch|Splunk|Loki|
|---|---|---|---|---|
|**Лицензия**|Elastic License (Исходно Apache 2.0, теперь проприетарная)|Apache License, Version 2.0 (Полностью открытая)|Проприетарная|Apache License 2.0|
|**Потребление дисковых ресурсов**|Высокое. Индексирует все поля, что приводит к большому объёму данных на диске (часто в 2-3 раза больше сырых логов). Использует сжатие, но общий объём значителен.|Аналогично ELK (высокое), так как наследует ту же модель данных и хранения.|Высокое. Эффективное сжатие, но также индексирует все данные, требуя значительного дискового пространства.|**Очень низкое**. Не индексирует содержимое логов, только их метки (labels). Хранит сырые логи в сжатом виде (часто в формате gzip), занимая значительно меньше места (примерно в 10-30 раз меньше, чем ELK/Splunk для тех же данных).|
|**Потребление процессорных ресурсов (CPU)**|Высокое. Требует значительных ресурсов для индексации (парсинг, анализ, создание инвертированных индексов) и выполнения сложных поисковых запросов.|Высокое. Схожая с ELK архитектура, аналогичные высокие требования.|Высокое. Оптимизирован, но для интенсивной индексации и поиска требуются мощные CPU.|**Низкое**. Отсутствие индексации содержимого радикально снижает нагрузку на CPU. Основные затраты — на сжатие данных и поиск по меткам. Поиск по содержимому (тексту) требует полного сканирования, что CPU-затратно, но обычно применяется ограниченно.|
|**Архитектура хранения**|Инвертированный индекс (по всем полям) + исходный JSON-документ.|Инвертированный индекс (по всем полям) + исходный JSON-документ.|Проприетарные сжатые индексы (все поля индексируются).|Сжатые файлы ("чанки") логов, индексируются **только метки** (labels). Содержимое логов не индексируется.|


